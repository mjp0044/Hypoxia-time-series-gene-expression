import(methods)
import(AnnotationDbi)

### Only put what is statically exported here. All the AnnObj instances
### created at load time are dynamically exported (refer to R/zzz.R for
### the details).
export(
    org.Tcalifornicus.eg,
    org.Tcalifornicus.eg_dbconn,
    org.Tcalifornicus.eg_dbfile,
    org.Tcalifornicus.eg_dbschema,
    org.Tcalifornicus.eg_dbInfo,

    org.Tcalifornicus.egORGANISM
)

